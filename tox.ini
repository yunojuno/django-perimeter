[tox]
envlist = py{36,37}-django{111,20,21,22}

[testenv]
deps =
    django111: Django==1.11
    django20: Django==2.0
    django21: Django==2.1
    django22: Django==2.2

commands=
    coverage erase
    coverage run --include=perimeter/* runtests.py
    coverage report -m
